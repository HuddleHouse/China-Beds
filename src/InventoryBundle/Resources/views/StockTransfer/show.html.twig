{% extends ':default:layout.html.twig' %}

{% block title %}New Stock Transfer{% endblock %}
{% block page_title %}
    New Stock Transfer
{% endblock page_title %}

{% block page_name %}
    <a href="{{ path('stocktransfer_index') }}">< Back to the list</a>
{% endblock page_name %}

{% block actions %}
{% endblock actions %}

{% block footer %}
    <script>
        $(document).ready(function () {
            $('#part-table').DataTable({
                paging: false
            });
        });
    </script>
{% endblock footer %}

{% block content %}
    <div class="table">
        <div class="col-md-12">
            <div class="table">
                <table id="part-table" class="table table-striped table-bordered dataTable no-footer" cellspacing="0" width="100%">
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ stockTransfer.id }}</td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td>{% if stockTransfer.date %}{{ stockTransfer.date|date('Y-m-d') }}{% endif %}</td>
                    </tr>
                    <tr>
                        <th>Message</th>
                        <td>{{ stockTransfer.message }}</td>
                    </tr>
                    </tbody>
                </table>
                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </div>
        </div>
    </div>
{% endblock %}
