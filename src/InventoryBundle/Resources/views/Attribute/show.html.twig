{% extends ':default:layout.html.twig' %}

{% block title %}Attribute list{% endblock %}
{% block page_title %}
    Attribute list
{% endblock page_title %}

{% block page_name %}
    <a href="{{ path('attribute_index') }}">< Back to the list</a>
{% endblock page_name %}

{% block actions %}
{% endblock actions %}

{% block footer %}
    <script>
        $(document).ready(function () {
            $('#part-table').DataTable({
            });
        });
    </script>
{% endblock footer %}

{% block content %}
    <h1>Attribute</h1>

    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ attribute.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ attribute.code }}</td>
            </tr>
            <tr>
                <th>image</th>
                <td><img src="{{ attribute.webPath }}"> </td>
            </tr>
        </tbody>
    </table>

        <a class="btn btn-success" href="{{ path('attribute_edit', { 'id': attribute.id }) }}">Edit</a>
            {{ form_start(delete_form) }}
                <input class="btn btn-warning" type="submit" value="Delete">
            {{ form_end(delete_form) }}
{% endblock %}
