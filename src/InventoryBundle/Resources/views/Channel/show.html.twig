{% extends ':default:layout.html.twig' %}

{% block title %}Specification{% endblock %}
{% block page_title %}
    Specification
{% endblock page_title %}

{% block page_name %}
    <a href="{{ path('admin_channel_index') }}">< Back to the list</a>
{% endblock page_name %}

{% block actions %}
{% endblock actions %}

{% block footer %}
    <script>
        $(document).ready(function () {
            $('#part-table').DataTable({
            });
        });
    </script>
{% endblock footer %}

{% block content %}
    <h1>Channel</h1>

    <table id="part-table" class="table table-striped table-bordered dataTable no-footer" cellspacing="0" width="100%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ channel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ channel.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ channel.url }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-default" href="{{ path('admin_channel_edit', { 'id': channel.id }) }}">Edit</a>
            {{ form_start(delete_form) }}
                <input style="margin: -55px 0 0 60px;" class="btn btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}
{% endblock %}
