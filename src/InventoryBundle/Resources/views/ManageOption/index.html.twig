{% extends ':default:layout.html.twig' %}

{% block title %}Manage Options{% endblock %}
{% block page_title %}
    Manage Options
{% endblock page_title %}

{% block page_name %}
    Assortment
{% endblock page_name %}

{% block actions %}
{% endblock actions %}

{% block footer %}
    <script>
        $(document).ready(function () {
            $('#part-table').DataTable({
            });
        });
    </script>
{% endblock footer %}


{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="table">
                <a class="btn btn-success" href="{{ path('manage_options_new') }}" style="margin-bottom: 15px;">Create a new option</a>

                <table id="part-table" class="table table-striped table-bordered dataTable no-footer" cellspacing="0" width="100%">
                    <thead>
                    <tr>
                        <th>Name</th>
                        <th>Code</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for manageOption in manageOptions %}
                        <tr>
                            <td><a href="{{ path('manage_options_edit', { 'id': manageOption.id }) }}">{{ manageOption.name }}</a></td>
                            <td>{{ manageOption.code }}</td>
                            <td>
                                <a class="btn btn-info btn-xs purple" href="{{ path('manage_options_edit', { 'id': manageOption.id }) }}"><i class="fa fa-edit"></i> edit</a>
                                <a class="btn btn-default btn-xs purple" href="{{ path('manage_options_show', { 'id': manageOption.id }) }}"><i class="fa fa-list"></i> show</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
