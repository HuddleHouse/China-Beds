{% extends 'base.html.twig' %}

{% block body %}
    <h1>ManageOption</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manageOption.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ manageOption.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ manageOption.code }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manage_options_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('manage_options_edit', { 'id': manageOption.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
