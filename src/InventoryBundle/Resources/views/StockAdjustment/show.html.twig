{% extends ':default:layout.html.twig' %}

{% block title %}New Adjustment{% endblock %}
{% block page_title %}
    New Adjustment
{% endblock page_title %}

{% block page_name %}
    <a href="{{ path('stockadjustment_index') }}">< Back to the list</a>
{% endblock page_name %}

{% block actions %}
{% endblock actions %}

{% block footer %}
    <script>
        $(document).ready(function () {
            $('#part-table').DataTable({
                paging: false
            });
        });
    </script>
{% endblock footer %}

{% block content %}
    <div class="table">
        <div class="col-md-12">
            <div class="table">
                <table id="part-table" class="table table-striped table-bordered dataTable no-footer" cellspacing="0" width="100%">
                    <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ stockAdjustment.id }}</td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td>{% if stockAdjustment.date %}{{ stockAdjustment.date|date('Y-m-d') }}{% endif %}</td>
                    </tr>
                    <tr>
                        <th>Message</th>
                        <td>{{ stockAdjustment.message }}</td>
                    </tr>
                    </tbody>
                </table>

                {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </div>
        </div>
    </div>
{% endblock %}
