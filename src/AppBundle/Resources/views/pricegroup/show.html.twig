{% extends 'base.html.twig' %}

{% block body %}
    <h1>PriceGroup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ priceGroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ priceGroup.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_price_group_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_price_group_edit', { 'id': priceGroup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
